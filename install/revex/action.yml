# SPDX-FileCopyrightText: Copyright 2026 Carabiner Systems, Inc
# SPDX-License-Identifier: Apache-2.0

name: setup revex
author: carabiner-dev
description: 'Installs the OpenVEX revex document fixer into the runner'
branding:
  icon: 'download-cloud'
  color: 'green'
inputs:
  install-dir:
    description: 'Path to install the binary'
    required: false
    default: '$HOME/.carabiner'
  version:
    description: 'revex version to install'
    required: false
    default: 'v0.0.2'
runs:
  using: "composite"
  steps:
    - uses: $/install/download-and-verify
      with:
        tool: revex
        version: ${{ inputs.version }}
        install-dir: ${{ inputs.install-dir }}
